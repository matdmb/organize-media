[lfs]
    # Limit concurrent transfers to reduce bandwidth usage
    concurrenttransfers = 1
    
    # Increase batch size to reduce the number of API calls
    batchsize = 100
    
    # Skip smudge during git pull/clone to improve performance
    # (Will only download LFS objects when explicitly requested or when fetched by git lfs pull)
    fetchexclude = * 